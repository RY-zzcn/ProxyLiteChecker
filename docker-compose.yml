services:
  proxylitechecker:
    build: .
    container_name: proxylitechecker
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8899:8899"
      - "18080:18080"
    volumes:
      - ./data:/app/data
