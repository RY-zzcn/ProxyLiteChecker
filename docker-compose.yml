services:
  proxylitechecker:
    build: .
    container_name: proxylitechecker
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8899:8899"
      - "127.0.0.1:18080:18080"
    volumes:
      - ./data:/app/data
